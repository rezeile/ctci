#include "node.h"


void partition(int x, node *head) {
	node* bigger, bigger_tail;
	node* smaller, smaller_tail;
	node *cur = head;

	while(cur != nullptr) {
		if(cur->data < x) {
			if(smaller) {
			cl	
		}
	}
}
